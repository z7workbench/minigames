import 'package:flutter/material.dart';

const titleTextStyle = TextStyle(fontSize: 24.0, color: Colors.black);
const subtitleTextStyleRed = TextStyle(fontSize: 20.0, color: Colors.red);
const subtitleTextStyleBlack = TextStyle(fontSize: 20.0, color: Colors.black);
const docTextStyle = TextStyle(fontSize: 16.0);

const containerPadding = EdgeInsets.all(16.0);
const margin = Padding(padding: EdgeInsets.all(6.0));