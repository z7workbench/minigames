import 'package:flutter/material.dart';

const titleTextStyle = TextStyle(fontSize: 24.0, color: Colors.black);
const docTextStyle = TextStyle(fontSize: 16.0);

const containerPadding = EdgeInsets.all(16.0);